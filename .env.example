# Klash needs no secrets in the frontend. The Intelligent Contract is the backend;
# the static SPA talks to it directly through genlayer-js. The only configuration is
# the deployed address, committed in frontend/src/lib/contract.ts. Never put a private key here.
